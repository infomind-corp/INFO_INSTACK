/**
 * 전역 설정 클래스 모음
 *
 * 예정 클래스:
 *   - SecurityConfig     (Spring Security, JWT 필터 체인)
 *   - DataSourceConfig   (DataSource, MyBatis)
 *   - CorsConfig         (CORS 설정)
 *   - SwaggerConfig      (SpringDoc OpenAPI)
 */
package infomind.instack.api.config;